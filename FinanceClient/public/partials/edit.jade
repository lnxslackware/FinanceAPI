section
    form(ng-controller="FormEditAccountController", name="formEditAccount", novalidate)
        fieldset.default
            legend.hidden Add student form
            div.combine
                label.default.top(for="name") Enter a name
                input#name.default(type="text", ng-model="account.name", name="name")
            div.combine
                label.default.top(for="grade") Enter a grade
                input#grade.default(type="text", ng-model="account.login", name="login")
            div.combine
                label.default.top(for="grade") Enter a password
                input#grade.default(type="text", ng-model="account.password", name="password")
            div.combine
                button.default.blue(ng-click="update()") Submit